// Analytics opt-in config (privacy-first).
//
// Ce fichier est OPTIONNEL — sans contenu, le site fonctionne sans tracker.
// Pour activer Plausible :
//   window.ANALYTICS_PLAUSIBLE_DOMAIN = 'harotensnor.github.io';
// Pour activer Cloudflare Insights :
//   window.ANALYTICS_CLOUDFLARE_TOKEN = '<token>';
//
// Le script tiers correspondant ne sera chargé qu'APRÈS consentement
// explicite de l'utilisateur via la bannière cookies (cf
// _maybeEnableAnalytics dans app.js).
//
// Par défaut : pas de tracking. Cette config est un placeholder vide
// pour éviter le 404 dans le Network panel — pas de fonctionnalité.
