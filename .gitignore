__pycache__/
*.pyc
auto_refresh.log
*.tmp
.DS_Store
data.json
# Cache local des sources externes (footballdata, MLB pitchers, tennis CSV).
# Re-téléchargeable, pas besoin de versionner.
.cache/
# Playwright / Node
node_modules/
playwright-report/
test-results/
.playwright/

# Cookie / credentials Winamax — JAMAIS COMMITES (auth tokens, faut pas
# que ca quitte la machine de Theo).
.winamax_session
.winamax_session.auto
.winamax_raw/
winamax_import.log
winamax_import.log.1
winamax_v20_admin.log

# winamax_my_bets.json + winamax_settings.json sont VOLONTAIREMENT
# commités — Theo a explicitement autorisé ("j'ai rien a cacher"),
# le bilan est ainsi visible sur GitHub Pages.

# AUDIT-2026-04-27 (Sprint 45 #24) — Archives backup data.js.
# Rolling 30j × ~250KB compressé = 7.5MB cumulés. Committer ces
# fichiers gonflerait le repo inutilement. backup_data_snapshot.py
# garde un rolling local; pour recovery passée, `git checkout` sur
# un commit historique de data.js est largement suffisant.
# Si backup explicit voulu, push manuellement avec git add -f.
archives/
